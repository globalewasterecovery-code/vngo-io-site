{
  "schema": "{task_id, assignee, repo, task, priority, status, note}",
  "policy": "Claude does NOT execute these directly — delegate to OpenClaw/Qwen/local models per user instruction (2026-08-29 Section 七). Claude only reviews/accepts results.",
  "tasks": [
    {"task_id": "T01", "assignee": "OpenClaw/Qwen", "repo": "vietnamzichan-site", "task": "Bulk-translate zh-TW/ko/ja content BLOCKED until dict-consolidation refactor lands (see CARBON_HANDOFF.md Update 2026-08-29) — do NOT start translation before the refactor ships, it will produce content with no safe place to land.", "priority": "P4", "status": "blocked"},
    {"task_id": "T02", "assignee": "OpenClaw/Qwen", "repo": "vietchiphub-v1", "task": "Bulk-translate ko/ja labels for build_site.py's localized_page() ternary label sets (structure is simpler than VietnamZiChan's — one ternary, not 10+ scattered dicts; still verify no KeyError risk before running).", "priority": "P5", "status": "pending"},
    {"task_id": "T03", "assignee": "OpenClaw/Qwen", "repo": "all 4 active", "task": "Missing-key scan across every site_i18n strings.json/registry.json pair; report gaps back into I18N_TRANSLATION_QUEUE.json.", "priority": "P3", "status": "pending"},
    {"task_id": "T04", "assignee": "OpenClaw/Qwen", "repo": "all 4 active", "task": "Sitemap validation (all URLs resolve 200, no orphaned/broken entries) — VNGO's sitemap.xml just gained 4 new URLs (golf-quote, team-building, corporate, partner) this pass.", "priority": "P3", "status": "pending"},
    {"task_id": "T05", "assignee": "OpenClaw/Qwen", "repo": "all 4 active", "task": "HTML static validation (W3C-style linting) on all generated/edited pages.", "priority": "P4", "status": "pending"},
    {"task_id": "T06", "assignee": "OpenClaw/Qwen", "repo": "vngo-io-site, vietnamzichan-site, vietchiphub-v1, carbon-base", "task": "Git-safety push retry — attempt `git push` when Mac network/GitHub proxy recovers; all 4 repos are clean, committed, and fast-forward-ready. carbon-base ALSO needs `git commit` first (blocked on missing git identity — see CURRENT_BLOCKERS in CARBON_MASTER_HANDOFF.md; do not set global git config, use an environment with identity already configured).", "priority": "P2", "status": "pending"},
    {"task_id": "T07", "assignee": "OpenClaw/Qwen", "repo": "all 4 active", "task": "Page health checks (200 status, no console errors) once each site is live on Netlify post-push.", "priority": "P3", "status": "pending"},
    {"task_id": "T08", "assignee": "OpenClaw/Qwen", "repo": "all 4 active", "task": "Duplicate-content scan across locale pages (VietnamZiChan/VietChipHub page-mode outputs) to catch generator drift.", "priority": "P4", "status": "pending"},
    {"task_id": "T09", "assignee": "OpenClaw/Qwen", "repo": "vngo-io-site", "task": "Generate realistic test submissions for VNGO's 5 new forms (Request-a-Quote /#inquiry, Golf Quote /golf-quote/, Team Building Quote /team-building/, MICE/Corporate /corporate/, B2B Partner /partner/) once deployed, to confirm Netlify Forms + Supabase leads table both receive records end-to-end. NO fabricated data should ever be written back into the live site's displayed content — this is submission testing only.", "priority": "P2", "status": "pending"},
    {"task_id": "T10", "assignee": "OpenClaw/Qwen", "repo": "all", "task": "Log cleanup / rotation across nodes and browser-agent scripts.", "priority": "P5", "status": "pending"},
    {"task_id": "T11", "assignee": "user or ops with Supabase dashboard access", "repo": "vngo-io-site", "task": "Apply supabase/migrations/20260829_create_leads.sql to the VNGO Supabase project (this session only has the anon publishable key, no DDL/service-role access). Once applied, T09's test submissions will start landing in the `leads` table with zero further code changes.", "priority": "P1", "status": "pending"}
  ]
}
